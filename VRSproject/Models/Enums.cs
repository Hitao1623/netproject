namespace VRSproject.Models;

public enum VehicleType { Car, Bike, Truck }
public enum VehicleStatus { Available, Reserved, Rented, UnderMaintenance }
public enum PricingType { Hourly, Daily, Weekly }
public enum ReservationStatus { Pending, Confirmed, Cancelled, Expired }
public enum RentalStatus { Active, Completed, Cancelled }
